[package]
name = "issuer"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7", features = ["multipart"] }
tokio = { version = "1", features = ["full"] }
bincode = "1.3"
serde_json = "1.0"
serde = "1.0"

tlsn-core = { path = "../tlsn/crates/core" }
